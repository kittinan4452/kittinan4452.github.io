/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/tii-kittinan-pages", // Uncomment for GitHub Pages deployment
    output: "export",
    images: {
        unoptimized: true,
      },
  };

  export default nextConfig;
  